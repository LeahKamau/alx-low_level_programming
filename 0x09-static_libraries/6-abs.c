#include "main.h"

/**
 * _abs - computes the absolute value
 *
 * @i: input number type int
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
